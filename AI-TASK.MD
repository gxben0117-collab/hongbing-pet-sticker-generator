# 紅兵寵物 LINE 貼圖生成器：介面與文字腳本優化分析

## 目前觀察

本專案目前是靜態單頁工具，主要程式、介面、資料與提示詞規則集中在 `index.html`。功能很完整，但日常使用時資訊量偏大，尤其 v1.35 新增大量角色、台詞與腳本群組後，使用者會比較難快速找到真正需要的設定。

主要痛點：

- 介面標題仍顯示 `v1.34`，README 顯示目前版本為 `v1.35`，版本資訊不一致。
- 上方已有「AI 圖片工作流」，第 1 步又再次說明圖片上傳，內容重複。
- 主流程有 9 個步驟，但多數日常生成只需要寵物資料、貼圖數量、台詞腳本與生成咒語。
- 畫風、服裝、道具、字體、構圖、背景屬於進階調整，全部攤開會讓操作流程變長。
- 腳本群組太多，且包含 `v1.31 新增`、`v1.32 新增` 這類維護用名稱，不適合直接出現在使用介面。
- 腳本區功能完整，但目前像後台資料管理工具，不像快速生成貼圖的主流程。
- `LINE 貼圖上架準備清單` 對生成咒語不是必要流程，適合保留但預設收合或移到說明區。

## 優化目標

讓使用者能用更少步驟完成主要任務：

1. 放入寵物資料。
2. 選擇貼圖數量。
3. 選擇或產生貼圖文字。
4. 組合並複製咒語。

同時保留目前已累積的大量角色、腳本與進階設定，但不要讓它們干擾主要操作。

## 方案 A：輕整理，快速改善

適合目標：快速變順手，不大改程式結構。

調整內容：

- 修正介面版本 `v1.34` 為 `v1.35`。
- 縮短 Hero 區與頁首文案，讓工具更快進入主流程。
- 將「AI 圖片工作流」與第 1 步寵物描述整合，避免重複。
- 將服裝、道具、字體、構圖、背景預設收進「進階設定」。
- 腳本群組移除或改名 `v1.31 新增`、`v1.32 新增` 等版本分類。
- `LINE 貼圖上架準備清單` 保留但預設收合，文案縮短。

優點：

- 工程量小。
- 風險低。
- 可快速改善視覺與操作雜訊。

缺點：

- 腳本資料仍然龐大。
- 介面本質仍是單頁長表單。

## 方案 B：推薦，重整成真正順手的生成工具

適合目標：保留功能，但把日常使用流程變簡單。

建議把主介面重整為 4 大區：

### 1. 寵物資料

- 上傳照片。
- 文字描述。
- 快速套餐。
- 我的套餐。

原本分散的圖片工作流與寵物描述可以合併在這裡。

### 2. 貼圖設定

只保留最常用設定在主畫面：

- 貼圖數量。
- 主要畫風。
- 是否使用純白背景。

其他項目放進「進階設定」：

- 服裝。
- 道具。
- 字體。
- 構圖。
- 背景細節。

### 3. 台詞腳本

將目前大量腳本群組重新整理成少數主分類：

- 常用短句。
- 情緒反應。
- 撒嬌陪伴。
- 工作日常。
- 家人朋友。
- 寵物專用。
- 純表情無字。
- 更多分類。

「更多分類」再放入較少用或特定用途內容：

- 商用客服。
- 多語貼圖。
- 指定寵物專屬。
- 12 生肖。
- 動物園系列。
- 海洋動物系列。

這樣日常使用時不會被 70 多個分類淹沒，但資料仍然保留。

### 4. 生成咒語

- 組合全部並複製。
- 顯示輸出字數。
- 顯示簡短使用提醒。
- 上架清單改為可展開說明，不放在主操作動線中。

優點：

- 最符合目前「功能太多、不順手」的問題。
- 不需要砍掉既有台詞與角色。
- 使用者能更快完成主要任務。
- 後續仍可逐步拆檔或模組化。

缺點：

- 需要調整較多 HTML 結構與部分 JS 初始化邏輯。
- 測試需要同步更新。

## 方案 C：深度整理，順便改善程式架構

適合目標：未來還會長期新增角色、台詞、風格與功能。

在方案 B 的基礎上，進一步拆分檔案：

- `index.html`：只保留主要 HTML 結構。
- `src/data.js`：放套餐、腳本分類、角色與風格資料。
- `src/main.js`：放介面互動邏輯。
- `src/promptEngine.js`：放咒語組合邏輯。
- `tests/`：補上資料完整性與咒語輸出測試。

優點：

- 長期維護性最好。
- 新增台詞與角色時比較不容易弄壞 UI。
- 測試可以更精準。

缺點：

- 工程量最大。
- 短期會動到較多檔案。
- 需要重新確認建置與 GitHub Pages 輸出流程。

## 建議選擇

建議選擇方案 B。

原因：

- 目前真正的問題是操作體驗與資訊架構，不是功能缺失。
- 方案 B 可以保留既有資料，又能把常用流程變短。
- 方案 B 不需要立刻做大規模架構拆分，風險比方案 C 低。
- 後續如果還要繼續擴充，再把方案 C 作為第二階段。

## 建議執行順序

1. 修正版本資訊與頁首文案。
2. 整合 AI 圖片工作流與寵物描述區。
3. 將主流程改成 4 大區。
4. 將細項設定收進「進階設定」。
5. 重整腳本分類顯示名稱與排序。
6. 將上架清單改為輔助說明區。
7. 執行測試與建置。

## 驗收重點

- 首屏進入後能快速看到主要生成流程。
- 使用者不用理解所有進階選項，也能產出咒語。
- 腳本分類不再出現版本名稱。
- 日常常用腳本更容易找到。
- 原本的指定寵物、生肖、動物園、海洋動物、多語與商用腳本仍可使用。
- `npm test`、`npm run build` 可正常通過。
