# 紅兵寵物 LINE 貼圖生成器 - 部署說明

## 線上網址
部署完成後，網站將可在以下網址訪問：
```
https://gxben0117-collab.github.io/hongbing-pet-sticker-generator/
```

## 部署步驟

### 1. 創建 GitHub 倉庫
1. 前往 GitHub：https://github.com/new
2. Repository name: `hongbing-pet-sticker-generator`
3. Description: `紅兵寵物 LINE 貼圖生成器`
4. 選擇 **Public**
5. 不要勾選任何初始化選項
6. 點擊 **Create repository**

### 2. 推送代碼到 GitHub
```bash
cd "C:\AIProjects\002專案進行中\紅兵寵物LINE貼圖生成器"
git add -A
git commit -m "feat: 初始提交 - 紅兵寵物LINE貼圖生成器 v1.34"
git branch -M main
git remote add origin https://github.com/gxben0117-collab/hongbing-pet-sticker-generator.git
git push -u origin main
```

### 3. 啟用 GitHub Pages
1. 前往倉庫 Settings
2. 左側選單找到 **Pages**
3. Source 選擇 **GitHub Actions**
4. 等待部署完成（約 1-2 分鐘）

### 4. 設置 Actions 權限
1. Settings > Actions > General
2. Workflow permissions 選擇 **Read and write permissions**
3. 勾選 **Allow GitHub Actions to create and approve pull requests**
4. 點擊 Save

## 自動部署
每次推送到 `main` 分支時，GitHub Actions 會自動部署。

## 注意事項
- 確保 `index.html` 在根目錄
- 所有資源使用相對路徑
- 第一次部署可能需要 5-10 分鐘
